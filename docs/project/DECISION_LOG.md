# Decision Log

| Date | Decision | Reason | Alternatives Considered | Status |
|---|---|---|---|---|
| 2026-09-10 | Migrate this repository onto AntBrainOS Project Starter Kit v3.10.0, profile `web_application`, via the kit's own governed adoption workflow (`adopt-audit` → `plan-migration` → `migrate --apply`) rather than hand-writing governance files. | User explicitly requested it, ahead of a planned landing-page redesign; the kit ships exactly this adoption path for an existing repo with no prior kit installation. | Hand-authoring governance docs directly; skipping adoption entirely. | Accepted |
| 2026-09-11 | Landing-page visual redesign ships as a new `docs/v0.3.1/index.html` (pure re-skin of `v0.2.5`, zero content change) rather than editing `v0.2.5` in place. | Matches this repo's own established versioning rule — new work is always a new version directory, prior versions stay frozen. | Editing `v0.2.5` in place; a `v0.3.0` directory (rejected — the git tag `v0.3.0` already names an unrelated governance-only commit, reusing it as a directory name risked confusion). | Accepted |
| 2026-09-11 | `docs/index.html` (confirmed to be the live site root) stays a version picker and was redesigned in place with a richer, full-width "infographic style" layout — not replaced with the STEM marketing content itself. | Explicit, repeated user decision across three feedback rounds. | Replacing it with the full site content (rejected by user); leaving it as a plain single-box card list (rejected as visually insufficient). | Accepted |
| 2026-09-11 | The redesigned `docs/index.html`'s 4 version cards use a tonal blue progression by age (breaking to orange only for the latest version), not 4 unrelated hues, and its release-timeline connector is flat/horizontal rather than a rising diagonal. | Honesty constraint: these are archived releases in chronological order, not a ranked quality ladder — `v0.3.1` is a re-skin of `v0.2.5`, not a feature upgrade, so a rising line or unrelated hues would assert an improvement claim with no basis. No per-version ratings/scores or feature checklists were added for the same reason (no verified feature facts exist for `v0.1.4` beyond line count and font/color tokens). | A literal port of the reference site's dot-rating comparison table and 3-tier hue system. | Accepted |
