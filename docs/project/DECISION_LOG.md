# Decision Log

| Date | Decision | Reason | Alternatives Considered | Status |
|---|---|---|---|---|
| 2026-09-10 | Migrate this repository onto AntBrainOS Project Starter Kit v3.10.0, profile `web_application`, via the kit's own governed adoption workflow (`adopt-audit` → `plan-migration` → `migrate --apply`) rather than hand-writing governance files. | User explicitly requested it, ahead of a planned landing-page redesign; the kit ships exactly this adoption path for an existing repo with no prior kit installation. | Hand-authoring governance docs directly; skipping adoption entirely. | Accepted |
| 2026-09-11 | Landing-page visual redesign ships as a new `docs/v0.3.1/index.html` (pure re-skin of `v0.2.5`, zero content change) rather than editing `v0.2.5` in place. | Matches this repo's own established versioning rule — new work is always a new version directory, prior versions stay frozen. | Editing `v0.2.5` in place; a `v0.3.0` directory (rejected — the git tag `v0.3.0` already names an unrelated governance-only commit, reusing it as a directory name risked confusion). | Accepted |
| 2026-09-11 | `docs/index.html` (confirmed to be the live site root) stays a version picker and was redesigned in place with a richer, full-width "infographic style" layout — not replaced with the STEM marketing content itself. | Explicit, repeated user decision across three feedback rounds. | Replacing it with the full site content (rejected by user); leaving it as a plain single-box card list (rejected as visually insufficient). | Accepted |
| 2026-09-11 | Added a whole-page decorative background field of education-themed inline SVG icons (graduation cap, flask, sigma, book, pencil, apple, diploma/scroll, atom, ruler — 9 icons reused ~2-3x each, ~19 instances) across every section, sized and colored to clearly read as imagery (opacity 0.10-0.18, 44-104px) while staying behind foreground content. Static placement, no new animation. Objects/symbols only, no human figures. | User feedback after seeing the re-architected page live: the background was "still plain and bland" and needed to "feel alive" with educational imagery. Kept to the existing inline-SVG system (no new dependency) and to the site's established color tokens only. Motion was deliberately not added in the same pass — the ask was for imagery, not more animation, and the page already animates the ring/orbs/dot/entrances; adding animated icons too was judged the most likely way this could tip into feeling busy. | A single large hero illustration (rejected — bigger risk of competing with the headline, and leaves the rest of the page untouched); a bottom-of-hero "bookshelf skyline" strip (rejected — narrower coverage than whole-page); including a human silhouette (rejected — harder to render well in minimal line art, and departs from the page's abstract/geometric icon style). | Accepted |
| 2026-09-11 | `docs/index.html` re-architected again: full-bleed centered hero (the previous two-column hero and its absolutely-positioned "stage" — CSS book, floating pills, overlapping ring — were removed), the latest release promoted to a full-width row above a 3-up row of the archived releases, and the 3-stat strip relocated to a closing band after the timeline. Every user-visible string kept byte-identical (verified: 49/49 strings, multiset-equal before and after). | User asked for a considerable visual enhancement and chose re-architecture over refinement. The old stage was also the never-visually-verified geometry flagged in `STATUS.md` — it used a hard-coded `right: 168px` to dodge the ring plus `white-space: nowrap`, and was fragile at intermediate widths; the replacement is entirely flow/grid-based and cannot collide. | Refining the existing layout in place (rejected as insufficient); keeping the stage and only re-lighting it. | Accepted |
| 2026-09-11 | The promoted latest release signals **recency, not rank**: width, first reading position, the already-approved orange accent, and a slow "breathing" dot — and deliberately *not* a larger badge, a bigger type scale, a trophy/star icon, `scale()`, or a glow the other cards lack. The timeline stays flat with no directional or sequential animation. | Preserves the 2026-09-11 honesty constraint below under a layout that could easily have implied a quality ladder. `v0.3.1` is a re-skin of `v0.2.5`; a "featured/best" reading would be an unfounded improvement claim. The 3-up row stays in reverse-chronological order for the same reason. | A "Featured" treatment with elevated styling (rejected — implies ranking). | Accepted |
| 2026-09-11 | All decorative emoji (🎓 ⚗️ ∑ 📖) replaced with hand-authored inline SVG in a `<symbol>` sprite, stroked with `currentColor` so each icon inherits its card's accent. | Emoji render differently on every OS (Segoe UI Emoji vs Apple), so the page's icon language was not under our control. Inline SVG is markup, not an icon library or CDN, so it respects the no-external-dependency rule. Departs from `v0.3.1`, which uses emoji in `color-mix` tinted chips — the chip treatment is kept, only the glyph source changed. | Keeping emoji inside the house chip treatment; pure-CSS shapes. | Accepted |
| 2026-09-11 | The redesigned `docs/index.html`'s 4 version cards use a tonal blue progression by age (breaking to orange only for the latest version), not 4 unrelated hues, and its release-timeline connector is flat/horizontal rather than a rising diagonal. | Honesty constraint: these are archived releases in chronological order, not a ranked quality ladder — `v0.3.1` is a re-skin of `v0.2.5`, not a feature upgrade, so a rising line or unrelated hues would assert an improvement claim with no basis. No per-version ratings/scores or feature checklists were added for the same reason (no verified feature facts exist for `v0.1.4` beyond line count and font/color tokens). | A literal port of the reference site's dot-rating comparison table and 3-tier hue system. | Accepted |
| 2026-09-12 | For the queued Firefox/Safari visual check, install real Firefox via Playwright and use Playwright's already-cached WebKit engine as an explicitly-labeled Safari proxy (not genuine Apple Safari), rather than skipping Safari or configuring a cloud device-farm service. | This machine has no real Firefox or Safari and no cross-browser cloud service configured; Firefox cost one ~90MB download, WebKit was free (already cached), and both give real signal as long as the Safari-proxy gap is stated plainly everywhere the result is recorded. | Manual-only check via the already-installed LibreWolf fork; skipping Safari entirely; standing up a BrowserStack/LambdaTest account for this session. | Accepted |
